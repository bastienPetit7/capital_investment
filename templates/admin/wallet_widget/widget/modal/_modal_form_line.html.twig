<div class="modal fade" id="modal-form-line" tabindex="-1" role="dialog" aria-labelledby="modal-form-line" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered" role="document">
        <div class="modal-content">
            <div class="modal-body p-0">
                <div class="card card-plain">
                    <div class="card-header pb-0 text-left">
                        <h3 class="font-weight-bolder text-info text-gradient"></h3>
                        <p class="mb-0">Add a line</p>
                    </div>
                    <div class="card-body">
                        {{ form_start(formLine) }}

                        {{ form_widget(formLine) }}
                        <button type="submit" class="btn btn-info">Add</button>
                        {{ form_end(formLine) }}

                    </div>

                </div>
            </div>
        </div>
    </div>
</div>
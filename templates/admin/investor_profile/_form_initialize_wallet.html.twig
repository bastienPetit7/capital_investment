<div class="col-12 ">
    <div class="card h-100">
        <div class="card-header pb-0">
            <h6 class="mb-0">Initialize the wallet</h6>
        </div>
        <div class="card-body p-3">
            {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-info">Create</button>
            {{ form_end(form) }}
        </div>
    </div>
</div>
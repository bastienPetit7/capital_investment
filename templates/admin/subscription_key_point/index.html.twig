{% extends "admin/base_admin.html.twig" %}

{% block body %}

    <div class="container-fluid py-4">
    
        <div class="row">
        
            <div class="col-12">
            
                <div class="card card-frame">
                
                    <div class="card-header">
                <a href=" {{path('admin_subscription_create')}} " class="btn btn-sm btn-dark">  < back </a>
                    
                            <h1>Subscription's KeyPoint index</h1>

                    </div>

                    <div class="card-body">
                    

                    

                        <table class="table">
                            <thead>
                                <tr>
                                 
                                    <th>Key Points</th>
                                    <th>actions</th>
                                </tr>
                            </thead>
                            <tbody>
                            {% for subscription_key_point in subscription_key_points %}
                                <tr>
                                   
                                    <td>{{ subscription_key_point.keyPoint }}</td>
                                    <td>
                                        <div class="d-flex  ">
                                        
                                            <a class="btn btn-sm btn-info me-3" href="{{ path('subscription_key_point_edit', {'id': subscription_key_point.id}) }}">edit</a>

                                            {{ include('admin/subscription_key_point/_delete_form.html.twig') }}

                                        </div>

                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="3">no records found</td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>

                        <a class="btn btn-info" href="{{ path('subscription_key_point_new') }}">Create new</a>  
                    
                    
                    </div>    

                
                
                
                
                </div>
            
            
            </div>
        
        </div>
    
    </div>
{% endblock %}




<form method="post" action="{{ path('subscription_key_point_delete', {'id': subscription_key_point.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ subscription_key_point.id) }}">
    <button class="btn btn-sm btn-danger">Delete</button>
</form>

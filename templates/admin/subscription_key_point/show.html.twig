<!DOCTYPE html>

<title>SubscriptionKeyPoint</title>

{% block body %}
    <h1>SubscriptionKeyPoint</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subscription_key_point.id }}</td>
            </tr>
            <tr>
                <th>KeyPoint</th>
                <td>{{ subscription_key_point.keyPoint }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('subscription_key_point_index') }}">back to list</a>

    <a href="{{ path('subscription_key_point_edit', {'id': subscription_key_point.id}) }}">edit</a>

    {{ include('admin/subscription_key_point/_delete_form.html.twig') }}
{% endblock %}
